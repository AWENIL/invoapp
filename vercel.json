{
  "version": 2,
  "installCommand": "if [ ! -d .flutter-sdk ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1 .flutter-sdk; fi",
  "buildCommand": "bash scripts/vercel_build.sh",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
