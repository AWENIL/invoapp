name: invo_common
description: Shared code for Invotaxi Flutter apps.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.13.1

dev_dependencies:
  flutter_lints: ^6.0.0
